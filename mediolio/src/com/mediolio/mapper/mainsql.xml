<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="main">
  
    <select id="select_m_nickname" resultType="string">
		SELECT m_nickname FROM MEMBER WHERE m_id=#{m_id}
	</select>
	<select id="mainProjects" resultType="pvo">
		select p.p_id, p.p_coverImg, p.p_title, m.m_studentID authorID, m.m_nickname authorName,
		(select count(*) from member_action where p_id=p.p_id) p_likenum, p.p_viewnum 
		from project p, member m where p.m_id=m.m_id order by p.p_date desc
	</select>
	<select id="projectHashtags" parameterType="int" resultType="hvo">
		select h_id, h_value from hashtag where p_id=#{p_id}
	</select>
  
  </mapper>
  