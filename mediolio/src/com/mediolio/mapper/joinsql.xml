<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="join">
  
    <select id="select_m_id" resultType="int">
		SELECT MAX(m_id) FROM MEMBER 
	</select>
  	<insert id="InsertJoinInfo" parameterType="mevo">
		INSERT INTO MEMBER VALUES(M_ID_SEQ.NEXTVAL, #{m_mail}, #{m_pw}, #{m_nickname}, #{m_gender}, #{m_studentID}, null, #{m_introduce}, SYSDATE)
	</insert>
  
  	<select id="doubleInfo" resultType="string" parameterType="string" >
		SELECT count(m_id) FROM ACCOUNT WHERE m_mail=#{m_mail}
	</select>
  </mapper>